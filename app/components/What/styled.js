import styled from 'styled-components'
import {
  theme, Flex, Body, Matrix, MatrixItem, MatrixItemIcon, MatrixItemText
} from '../../global-styles'

export const Root = styled.section`
  text-align: center;
`

export const Commercial = styled.iframe`
  border-radius: 20px;
  border: 2px solid ${theme.main};
  margin: 0 0 40px;
`
