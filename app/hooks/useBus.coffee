import React, {useLayoutEffect} from 'react'

export default (listeners) =>
  useLayoutEffect (=>
    upon event, listener for event, listener of listeners
    => end event, listener for event, listener of listeners
  ), []
